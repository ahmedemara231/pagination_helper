name: easy_pagination
description: "A flexible and customizable Flutter package for handling paginated data with minimal setup."
version: 0.0.3
homepage: https://github.com/ahmedemara231/pagination_helper
repository: https://github.com/ahmedemara231/pagination_helper
#publish_to: https://pub.dev

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  lottie: ^3.3.1
  fluttertoast: ^8.2.6
  connectivity_plus: ^5.0.2
  custom_refresh_indicator: ^4.0.1
  dio: ^5.8.0+1
  http: ^1.3.0
#  multiple_result: ^5.1.0
#  skeletonizer: ^1.4.3


  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


# The following section is specific to Flutter packages.
flutter:

  # To add assets to your package, add an assets section, like this:
   assets:
     - assets/lottie
